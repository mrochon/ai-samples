{
  "schema": 1,
  "description": "Given a query and a list of available database tables, detect which tables this query needs",
  "execution_settings": {
    "default": {
      "max_tokens": 100,
      "temperature": 0.1,
      "top_p": 1.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "query",
      "description": "The definition based on which a list of likely tables is to be extracted",
      "default": "",
      "is_required": true
    }
  ]
}